package cc.truecredits.truecreditlms.domain.acsreport.practice;

public class RuleService {
    private int id = 1;
    private String name = "name";
    Rule rule = new Rule(id, name);

    Rule rule1 = new Rule();

    Rule rule2 = new Rule(id, name);
}
